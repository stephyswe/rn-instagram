import {View, Text} from 'react-native';
import colors from '../../theme/colors';

const CreatePostScreen = () => {
  return (
    <View>
      <Text style={{color: colors.black}}>CreatePostScreen</Text>
    </View>
  );
};

export default CreatePostScreen;
